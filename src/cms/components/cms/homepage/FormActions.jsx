import React, { useState } from "react";
import { Save } from "lucide-react";

export default function FormActions({ onSave }) {
  // Returns nothing since the Save button has been moved to the page header globally.
  return null;
}
